# Simple Reactjs Todo with express and json file
1. Clone Project
2. run `npm install`
3. run `npm start` or `npm run build` 

# Data
Using json file
file location *data/lists.json*

# Server
run `node server/server.js`